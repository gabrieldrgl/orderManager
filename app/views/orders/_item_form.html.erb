<div class="nested-form-wrapper" data-new-record="<%= f.object.new_record? %>">
  <div>
    <h3>Item:</h3>
    <button type="button" data-action="nested-form#remove" class="btn btn-danger">Remove</button>
  </div>


  <%= f.label :name, class: "form-label" %>
  <%= f.text_field :name, class: "form-control mb-3" %>

  <%= f.label :quantity, class: "form-label" %>
  <%= f.text_field :quantity, class: "form-control mb-3" %>

  <%= f.label :unit_price, class: "form-label" %>
  <%= f.text_field :unit_price, class: "form-control mb-3" %>

  <%= f.label :discount, class: "form-label" %>
  <%= f.text_field :discount, class: "form-control mb-3" %>

  <%= f.hidden_field :_destroy %>
</div>
